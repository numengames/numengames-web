{
  "framework": "astro",
  "buildCommand": "pnpm build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!assets/|icons/|_astro/|favicon.ico|robots.txt|sitemap-index.xml).*)",
      "destination": "/"
    }
  ]
}