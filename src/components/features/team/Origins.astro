---
import Container from "@components/Container.svelte";
import { getLocaleFromURL, DEFAULT_LOCALE } from "../../../i18n";

import OriginsSection from "./Origins.svelte";

const { textObj } = Astro.props || {};
const currentLocale = getLocaleFromURL(Astro.url.pathname) || DEFAULT_LOCALE;
---

<Container className="flex flex-col items-center justify-center pt-24" bgColor="bg-[#1E1A17]">
	<OriginsSection {textObj} locale={currentLocale} />
</Container>
