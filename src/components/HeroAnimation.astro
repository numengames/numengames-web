<section class="hero-tunnel" aria-hidden="true">
	<div class="hero-tunnel__glow"></div>

	<div class="hero-tunnel__stack">
		<div class="hero-tunnel__frame hero-tunnel__frame--1"></div>
		<div class="hero-tunnel__frame hero-tunnel__frame--2"></div>
		<div class="hero-tunnel__frame hero-tunnel__frame--3"></div>
		<div class="hero-tunnel__frame hero-tunnel__frame--4"></div>
		<div class="hero-tunnel__frame hero-tunnel__frame--5"></div>
		<div class="hero-tunnel__frame hero-tunnel__frame--6"></div>
		<div class="hero-tunnel__frame hero-tunnel__frame--7"></div>
		<div class="hero-tunnel__frame hero-tunnel__frame--8"></div>
		<div class="hero-tunnel__frame hero-tunnel__frame--9"></div>
		<div class="hero-tunnel__frame hero-tunnel__frame--10"></div>
	</div>

	<img
		src="/assets/Logo_Khepri_Web.png"
		alt="Numen Games"
		class="hero-tunnel__logo"
	/>
</section>

<style>
	.hero-tunnel {
		position: absolute;
		inset: 0;
		overflow: hidden;
		pointer-events: none;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	/* GLOW CENTRAL MÁS INTENSO */

	.hero-tunnel__glow {
		position: absolute;
		left: 50%;
		top: 50%;
		width: min(36vw, 440px);
		height: min(36vw, 440px);
		transform: translate(-50%, -50%);
		border-radius: 9999px;

		background:
			radial-gradient(
				circle,
				rgba(255, 215, 120, 0.42) 0%,
				rgba(255, 210, 110, 0.28) 22%,
				rgba(217, 184, 106, 0.12) 40%,
				rgba(217, 184, 106, 0.04) 60%,
				rgba(217, 184, 106, 0) 78%
			);

		filter: blur(28px);
		opacity: 1;
	}

	.hero-tunnel__stack {
		position: absolute;
		left: 50%;
		top: 50%;
		width: min(42vw, 520px);
		height: min(42vw, 520px);
		transform: translate(-50%, -50%);
		perspective: 1200px;
		transform-style: preserve-3d;
	}

	/* FRAMES DEL TÚNEL */

	.hero-tunnel__frame {
		position: absolute;
		left: 50%;
		top: 50%;
		width: 44%;
		height: 60%;
		border-radius: 24px;

		transform: translate(-50%, -50%) scale(0.18);
		opacity: 0;

		border: 1px solid rgba(255, 215, 120, 0.6);

		box-shadow:
			0 0 40px rgba(255, 215, 120, 0.18),
			0 0 70px rgba(217, 184, 106, 0.14),
			inset 0 0 22px rgba(255, 215, 120, 0.18);

		background:
			linear-gradient(
				180deg,
				rgba(255, 215, 120, 0.08) 0%,
				rgba(217, 184, 106, 0.03) 25%,
				rgba(18, 18, 20, 0.10) 100%
			);

		animation: tunnelTravel 8s linear infinite;
		will-change: transform, opacity;
	}

	.hero-tunnel__frame::before {
		content: "";
		position: absolute;
		inset: 10%;
		border-radius: 18px;

		border: 1px solid rgba(255, 215, 120, 0.35);

		box-shadow:
			0 0 20px rgba(255, 215, 120, 0.16);
	}

	.hero-tunnel__frame::after {
		content: "";
		position: absolute;
		inset: 24%;
		border-radius: 12px;

		border: 1px solid rgba(255, 215, 120, 0.22);
	}

	.hero-tunnel__frame--1  { animation-delay: 0s; }
	.hero-tunnel__frame--2  { animation-delay: -0.8s; }
	.hero-tunnel__frame--3  { animation-delay: -1.6s; }
	.hero-tunnel__frame--4  { animation-delay: -2.4s; }
	.hero-tunnel__frame--5  { animation-delay: -3.2s; }
	.hero-tunnel__frame--6  { animation-delay: -4s; }
	.hero-tunnel__frame--7  { animation-delay: -4.8s; }
	.hero-tunnel__frame--8  { animation-delay: -5.6s; }
	.hero-tunnel__frame--9  { animation-delay: -6.4s; }
	.hero-tunnel__frame--10 { animation-delay: -7.2s; }

	@keyframes tunnelTravel {

		0% {
			transform: translate(-50%, -50%) scale(0.16);
			opacity: 0;
		}

		10% {
			opacity: 0.35;
		}

		40% {
			opacity: 0.32;
		}

		65% {
			transform: translate(-50%, -50%) scale(1.4);
			opacity: 0.26;
		}

		85% {
			transform: translate(-50%, -50%) scale(3);
			opacity: 0.12;
		}

		100% {
			transform: translate(-50%, -50%) scale(4.4);
			opacity: 0;
		}
	}

	/* LOGO EN EL PUNTO DE FUGA */

	.hero-tunnel__logo {
		position: absolute;
		left: 50%;
		top: 50%;
		transform: translate(-50%, -50%);
		width: min(5vw, 48px);
		height: auto;
		mix-blend-mode: screen;
		opacity: 0.85;
		pointer-events: none;
		z-index: 2;
	}

	/* TABLET */

	@media (max-width: 1279px) {

		.hero-tunnel__glow {
			top: 58%;
			width: min(52vw, 360px);
			height: min(52vw, 360px);
		}

		.hero-tunnel__stack {
			top: 58%;
			width: min(70vw, 420px);
			height: min(70vw, 420px);
		}

		.hero-tunnel__frame {
			width: 50%;
			height: 66%;
		}

		.hero-tunnel__logo {
			top: 58%;
			width: min(8vw, 56px);
		}
	}

	/* MOBILE */

	@media (max-width: 767px) {

		.hero-tunnel__glow {
			top: 64%;
			width: min(70vw, 260px);
			height: min(70vw, 260px);
			opacity: 0.9;
		}

		.hero-tunnel__stack {
			top: 64%;
			width: min(84vw, 300px);
			height: min(84vw, 300px);
		}

		.hero-tunnel__frame {
			width: 56%;
			height: 72%;
			border-color: rgba(255, 215, 120, 0.5);
		}

		.hero-tunnel__logo {
			top: 64%;
			width: min(12vw, 44px);
		}
	}
</style>
