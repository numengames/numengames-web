---
import Container from "@components/Container.svelte";
import { getLocaleStaticPaths, type SupportedLocale } from "../../../i18n";
import AboutNuminia from "./_About.svelte";

const { textObj } = Astro.props || {};

export const getStaticPaths = getLocaleStaticPaths;

const { locale } = Astro.params;
const currentLocale = (locale as SupportedLocale) || 'en';
---

<Container
	class="flex flex-col items-center justify-center pt-24 xl:pt-28"
	bgColor="bg-[#171717]">
	<AboutNuminia {textObj} client:only="svelte" />
</Container>
