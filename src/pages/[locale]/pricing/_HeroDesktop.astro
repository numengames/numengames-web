---
import { getLocaleStaticPaths } from "../../../i18n";
import Container from "@components/Container.svelte";

import HeroDesktopServices from "./_HeroDesktop.svelte";

import type { ContentData } from "@app-types/content";

interface Props {
	textObj: ContentData;
	class?: string;
}

const { textObj } = Astro.props;

export const getStaticPaths = getLocaleStaticPaths;
---

<Container className="flex flex-col items-center justify-center pt-16 xl:pt-20 pb-10" bgColor="bg-[#1E1A17]">
	<HeroDesktopServices {textObj} client:only="svelte" />
</Container>
