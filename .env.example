# Numen Games — variables de entorno
#
# El sitio no necesita ninguna en local. En CI, deploy.yml inyecta
# WORKERS_CI_COMMIT_SHA (sello de build, /version.json y pie de página).
#
# PUBLIC_WEB3FORMS_KEY, PUBLIC_GA_ID, PUBLIC_AI_* se retiraron el 2026-09-16:
# ningún fichero de src/ las leía.
